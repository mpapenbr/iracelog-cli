defaultLevel: info
loggers:
  "stress.worker": "info"
  "stress.processor": "info"
  "replay": "info"
  "replay.fetcher.driver": "info"
  "replay.state": "info"
  "webclient.driver": "info"
  # "dummy": "error"
